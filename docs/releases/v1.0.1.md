# v1.0.1

Repository polish release after the initial public launch.

## Highlights

- Added `MIT License` to the repository
- Added a macOS GitHub Actions smoke-test workflow
- Added `requirements-dev.txt` for local validation and CI
- Added `scripts/smoke_test.sh` for reusable PDF watermark verification
- Updated app packaging so the built `.app` bundle carries the project `LICENSE`

## Validation

- Local smoke test passed
- Local app build passed
- Local release bundle build passed
- GitHub Actions `CI` workflow passed on `main`

## Assets

- `word-pdf-watermark-macos-1.0.1.zip`
- `SHA256SUMS.txt`
